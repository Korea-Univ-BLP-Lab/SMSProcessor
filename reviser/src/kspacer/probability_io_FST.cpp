#include "definitions.h"
#include "report.h"

#define IS_HANGULE(str0, str1) ( (str0 >= 0xB0) && (str0 <= 0xC8) && (str1 >= 0xA1) && (str1 <= 0xFE) )  


/******************************************************************************/
/* 확률을 입력 (text 모드) */
void scan_probability_FST(FILE *fp, PROB_MAP_FST &probs) {
	
  
	// not use
   
}





